[project]
name = "shell"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pylint>=4.0.5",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.1.0",
    "ruff>=0.15.14",
]
